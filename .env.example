# Supabase Backend Configuration
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Note: Access PIN verification is enforced server-side by Edge Functions via the APP_PIN_HASH secret.
# Set APP_PIN_HASH in Supabase Dashboard (Project Settings > Edge Functions Secrets).
